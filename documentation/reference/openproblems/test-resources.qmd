---
title: Test resources
---

Root path: `resources_test`.

This directory contains the test resources required for running unit tests on the Viash components. 

:::{.callout-note}
These test resources are not stored within the git repository. Instead, they are obtained by running the sync resources script in the `scripts` directory.
:::