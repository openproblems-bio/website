---
title: Select datasets
order: 25
engine: knitr
---

```{r setup-packages, include=FALSE}
library(tidyverse)
library(rlang)

strip_margin <- function(text, symbol = "\\|") {
  str_replace_all(text, paste0("(\n?)[ \t]*", symbol), "\\1") 
}
```

{{< include /_include/_clone_repo.qmd >}}
{{< include /_include/_evaluate_code.qmd >}}
{{< include /_include/_read_api.qmd >}}

:::{.callout-important}
This page is still under construction.
:::


```{r, include=FALSE}
api_dataset <- read_anndata_spec("src/datasets/api/anndata_common_dataset.yaml")
```

## File format of a `r api_dataset$info$label`

`r api_dataset$info$description`

Example format:

```{r, echo=FALSE, output="asis"}
cat(paste0(format_slots(api_dataset), "\n"))
```

```{r, echo=FALSE}
format_slots_as_kable(api_dataset)
```


:::{.content-hidden}

## Browse existing datasets

## Create a dataset processor

### Define the API
* Which fields are required

### Create the viash component
* Create split dataset component

:::