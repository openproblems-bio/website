```{=html}
<div class="grid people-widget quarto-listing-cols-3">
<% for (const item of items) { %>
  <div class="people-person">
    <a href="<%- item.path %>">
      <img loading="lazy" class="avatar avatar-circle" src="<%- item.image %>"/>
    </a>
    <div class="portrait-title">
      <h5><a href="<%- item.path %>"><%= item.title %></a></h5>
      <% if (item.role) { %>
      <h6><%= item.role %></h6>
      <% } %>
      <% for (const organization of item.organizations || []) { %>
      <h6>
      <%= organization.role %>
      <% if (organization.name) { %> at <%= organization.name %> <% } %>
      </h6>
      <% } %>
      <% for (const link of item.about.links || []) { %>
      <a class="network-icon" href="<%- link.href %>">
        <i class=" bi-<%- link.icon %>"></i>
      </a>
      <% } %>
    </div>
  </div>
<% } %>
</div>
```